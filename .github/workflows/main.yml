name: Workflow 
 
on:
  release:
    types: [created]
  push:
    branches:
      - main
      - develop
      - feature
  pull_request:
    branches:
      - main
      - develop
         - feature

jobs:
  test:
    runs-on: ubuntu-latest  # Of ubuntu-latest/macOS-latest als specifiek platform nodig is
    steps:
      - name: Haal de repository op
        uses: actions/checkout@v4
 
      - name: Installeer .NET SDK
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'  # Gebruik de juiste versie voor MAUI
 
      - name: Installeer MAUI workloads
        run: dotnet workload install maui-android
 
      - name: Herstel afhankelijkheden
        run: dotnet restore
 
      - name: Bouw de applicatie
        run: dotnet build -f net8.0-android --configuration Release --no-restore
 
      - name: Voer unit tests uit
        run: dotnet test -f net8.0-android --configuration Release --no-restore --no-build --verbosity normal
